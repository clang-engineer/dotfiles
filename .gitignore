# tags
tags
.DS_Store

# SSH keys / sensitive — machine-specific, never commit (any name, repo-wide)
id_*
github_actions
github_actions.pub
known_hosts*
*.pem
*.key
authorized_keys
# SSH host configs overlaid from the private `secrets` repo (only 00-global tracked)
chezmoi/private_dot_ssh/private_config.d/[1-9]*
packages/Brewfile.lock.json

*.swp

# secrets (tokens, credentials)
.secrets
.secrets.ps1

# nvim DB connections are private (overlaid from the secrets repo) — only db/init.lua is tracked.
# Any subdirectory here (connections/, backups, etc.) must never be committed.
nvim/lazy/lua/user/db/*/

# claude code: ignore everything, allow only the public scaffold.
# personal instructions (CLAUDE.md) and vault/blog-bound skills stay out of the
# public repo — the private `secrets` overlay drops them into ~/.claude locally.
claude/*
!claude/README.md
!claude/keybindings.json
!claude/check-env.sh
!claude/link-commands.sh
!claude/statusline.sh
!claude/commands/
claude/commands/*
!claude/commands/commit-push.md

# local tool settings
.claude/settings.local.json
.serena/
