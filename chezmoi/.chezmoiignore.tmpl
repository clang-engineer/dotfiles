{{ if ne .chezmoi.os "windows" }}
# Windows-only — ignored on macOS/Linux
AppData
Documents
15-windows-install.ps1
{{ else }}
# Unix-only — ignored on Windows
.secrets
.zshrc
.bashrc
.bash_profile
.tmux.conf
.aerospace.toml
.opencode_aliases
.hammerspoon
.ssh
.config/nvim
05-brew-bundle.sh
10-install-shell-tools.sh
20-generate-claude-settings.sh
{{ end }}
# SSH host configs overlaid by the private secrets repo (both OS)
.ssh/config.d/[1-9]*
