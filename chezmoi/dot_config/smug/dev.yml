# 좌우 2분할, 왼쪽 pane에 claude 자동 실행
# 실행: smug switch dev   (tmux 안에서는 start 아닌 switch)
session: dev
root: ~/
windows:
  - name: dev
    layout: even-horizontal
    commands:
      - claude
    panes:
      - commands: []
