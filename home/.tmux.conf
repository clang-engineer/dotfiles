## Base behavior
set -sg escape-time 0 # remove ESC key delay
set -g default-terminal "tmux-256color" # advertise 256color + true color capable term
set -ga terminal-overrides ",*256col*:Tc" # enable true color passthrough
set -g mouse on # enable mouse interactions

## Pane behavior
bind '"' split-window -v -c "#{pane_current_path}" # vertical split in current path
bind % split-window -h -c "#{pane_current_path}" # horizontal split in current path
set -g pane-border-status bottom # show pane border status line
set -g pane-border-format " #P #{pane_current_command} " # show pane index + command
setw -g mode-keys vi # use vi-style copy mode keys

## Session tree ordering
bind s choose-tree -Zs -O name # session tree sorted by name

## TPM plugins
set -g @plugin 'tmux-plugins/tpm' # tmux plugin manager
set -g @plugin 'tmux-plugins/tmux-sensible' # for sane tmux defaults
set -g @plugin 'christoomey/vim-tmux-navigator' # for navigating panes and vim/nvim with Ctrl-hjkl
set -g @plugin 'jimeh/tmux-themepack' # to configure tmux theme
set -g @plugin 'tmux-plugins/tmux-resurrect' # persist tmux sessions after computer restart
set -g @plugin 'tmux-plugins/tmux-continuum' # automatically saves sessions for you every 15 minutes
set -g @themepack 'powerline/default/cyan' # use this theme for tmux
set -g @resurrect-capture-pane-contents 'on' # allow tmux-ressurect to capture pane contents
set -g @continuum-restore 'on' # enable tmux-continuum functionality

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

# tmux source-file ~/.tmux.conf # reload tmux config
