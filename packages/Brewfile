tap "FelixKratz/formulae", trusted: { formula: "borders" }

# Core tools
brew "git"
brew "neovim"
brew "tmux"
brew "sesh"            # tmux 스마트 세션 매니저 — fzf+zoxide 피커로 디렉토리/git repo 세션 점프 (tmux prefix+T)
brew "fzf"              # Generic fuzzy picker (Ctrl+T files, Alt+C directories; Ctrl+R is owned by Atuin)
brew "atuin"            # Structured shell history; replaces fzf Ctrl+R history search
brew "yazi"             # Terminal file manager; `y` wrapper hands final cwd back to the shell
brew "ripgrep"          # rg: blazing-fast text search. neovim grep backend
brew "fd"               # fd: intuitive file search (find replacement). Used by neovim file navigation
brew "lazygit"          # Git TUI client
brew "lazydocker"       # Docker/Compose TUI; useful when managing containers from tmux
brew "git-delta"        # delta: syntax-highlighting pager for git diff. Used by .gitconfig core.pager
brew "jq"               # CLI JSON filter/transform. Used for JSON parsing in scripts
brew "bat"              # cat replacement (syntax highlight, line numbers). Used by fzf preview
brew "tree"             # Print directory tree
brew "eza"              # ls replacement (color, git status, icons)
brew "gh"               # GitHub CLI (PR/issue/Actions)
brew "tldr"             # Condensed man pages — just the key examples
brew "borders"          # JankyBorders: highlight the focused macOS window; started by AeroSpace

# Shell/config quality — global Git hook delegates common checks to prek
brew "shellcheck"       # Shell static analysis
brew "shfmt"            # Deterministic shell formatting
brew "typos-cli"        # Fast source/config/document typo checker (`typos` command)
brew "stylua"           # Lua formatter/checker for Neovim/Hammerspoon configuration
brew "prek"             # Fast pre-commit-compatible hook runner

# General-purpose development utilities
brew "watchexec"        # Re-run arbitrary commands when files change
brew "hyperfine"        # Reproducible command/CLI benchmarks

# zsh plugins (sourced directly in .zshrc — no framework)
brew "zsh-autosuggestions"       # fish-style suggestions from history
brew "zsh-syntax-highlighting"   # fish-style command syntax highlighting

# Runtime version management — mise owns node/ruby/java (see ~/.config/mise/config.toml)
brew "mise"
brew "jdtls"             # Eclipse JDT language server (nvim LSP; not a runtime)

# Optional, depending on your needs
brew "zoxide"            # z: smart directory jump (autojump replacement)
brew "git-lfs"           # Not needed if you have no projects with large files
brew "libpq"             # Includes the psql CLI. Not needed if you don't use PostgreSQL
brew "uv"                # Fast Python package/venv manager — owns Python (mise handles node/ruby/java)
brew "wget"              # File downloader. curl is built in by default

# Future candidates — uncomment/configure only when the workflow is needed
# brew "mergiraf"        # Syntax-aware Git merge; register as a Git merge driver after install
# Neotest                 # LazyVim test.core extra, not a Homebrew package; add language adapters as needed
# PSScriptAnalyzer        # Windows/PowerShell only; manage from the Windows setup rather than this Brewfile

# Future terminal TUI candidates — see devkit/cheatsheets/terminal-tooling.md
# lazyjournal             # Logs TUI: journald plus container/Kubernetes logs; useful for server operations
# brew "k9s"             # Kubernetes TUI; add when Kubernetes becomes part of the regular workflow
# brew "rainfrog"        # Database TUI for browsing/querying SQL databases from the terminal
# brew "btop"            # Interactive CPU/memory/process monitor
# gh-dash                 # GitHub PR/issue dashboard TUI; add if `gh` alone becomes cumbersome
# Posting / ATAC          # HTTP API TUI clients; choose one only if terminal API testing becomes frequent
