# Shared Coding-Agent Configuration

Public prompts and utilities that work across coding agents live here. Chezmoi
links each command into the native location expected by Claude Code, Codex, and
OpenCode. Agent-specific settings remain in their own directories.

Private rules and commands are installed separately by the companion `secrets`
repository.
