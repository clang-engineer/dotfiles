
## pem-sso-guide 폴더를 toolbox로 이동

## Context

`ResearchEx/docs/pem-sso-guide/`는 협력업체(PEM 시스템 측)에 전달할 자료 (가이드 + 예제 코드 + 공개키)다.
git 관리 대상인 ResearchEx 리포 안에 있기보다 개인 toolbox에 두는 것이 적절하다.
이미 plan 파일(`toolbox/docs/plans/2026-04-16-researchex-pem-sso.md`)이 toolbox에 있으니, 관련 산출물도 같이 묶어둔다.

## 작업

### 이동
- From: `/Users/zero/Desktop/_zero/workspace/ResearchEx/docs/pem-sso-guide/`
- To:   `/Users/zero/Desktop/_zero/toolbox/docs/pem-sso-guide/`

### 보존할 파일 (폴더 내 그대로 이동)
- `README.md`
- `idp_public_key.pem`
- `PemSsoCryptoUtil.java`
- `PemSsoExample.java`

### 정리
- `ResearchEx/docs/.DS_Store` 및 `pem-sso-guide/.DS_Store` 정리 (이동에서 제외)
- 이동 후 빈 `ResearchEx/docs/` 폴더 삭제 (다른 파일이 없다면)

### Plan 파일 경로 업데이트
`toolbox/docs/plans/2026-04-16-researchex-pem-sso.md`의 "주요 파일 위치" 표에서 `pem-sso-guide/` 경로를 새 위치로 수정.

## 검증

```bash
ls /Users/zero/Desktop/_zero/toolbox/docs/pem-sso-guide/
ls /Users/zero/Desktop/_zero/workspace/ResearchEx/docs/ 2>/dev/null
```

새 위치에 4개 파일이 있고, 기존 ResearchEx/docs 는 비어있거나 삭제된 상태여야 함.

## 메일 발송 시

zip 파일이 필요하면 새 위치에서 생성:
```bash
cd /Users/zero/Desktop/_zero/toolbox/docs
zip -r pem-sso-guide.zip pem-sso-guide/
```

